// Working with numbers

#include <stdio.h>
#include <math.h>

int main(){
	
	// elevação
	printf("%f\n", pow(4, 3) ); // (a, b) -> a ** b

	// raiz quadrada
	printf("%f\n", sqrt(36) ); // (a) -> a ** 1/2
	
	return 0;
}